#version 150
precision highp float;

#include "planet\include\uniforms.h"

in  vec2 Vertex;
out vec3 vViewWS;
out vec2 vVertexS;

void main()
{
#include "planet\include\getspaceathmovertex.h"

 vVertexS = Vertex;
 vViewWS  = vVertex.xyz - vCamera;

 gl_Position = mModelViewProjection * vVertex;
}
